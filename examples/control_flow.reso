def main() -> i32:
    println("=== Control Flow in Reso ===")
    
    # Simple If Statement
    println("=== Simple If Statement ===")
    
    var temperature: i32 = 25
    
    if temperature > 30:
        println("It's hot outside!")
    
    if temperature < 10:
        println("It's cold outside!")
    
    if temperature >= 20 and temperature <= 30:
        println("Nice weather today!")
    
    # If-Else Statement
    println("=== If-Else Statement ===")
    
    var score: i32 = 85
    
    if score >= 60:
        println("You passed the exam!")
    else:
        println("You failed the exam.")
    
    # If-Else If-Else Chain
    println("=== If-Else If-Else Chain ===")
    
    var grade: i32 = 62
    
    if grade >= 90:
        println("Grade A - Excellent!")
    else if grade >= 80:
        println("Grade B - Good!")
    else if grade >= 70:
        println("Grade C - Average")
    else if grade >= 60:
        println("Grade D - Below Average")
    else:
        println("Grade F - Failed")
    
    # Nested If Statements
    println("=== Nested If Statements ===")
    
    var age: i32 = 25
    var has_license: bool = true
    var has_car: bool = false
    
    if age >= 18:
        println("You are an adult")
        
        if has_license:
            println("You can legally drive")
            
            if has_car:
                println("You can drive your own car")
            else:
                println("You need to borrow or rent a car")
        else:
            println("You need to get a driver's license")
    else:
        println("You are too young to drive")
    
    # Ternary Operator (if-else expression)
    println("=== Ternary Operator ===")
    
    var number: i32 = 15
    var parity = "even" if (number rem 2) == 0 else "odd"
    println("The number ".append(number.to_string())
        .append(" is ")
        .append(parity))
    
    var max_value = 100 if 100 > 50 else 50
    println("Maximum value: ".append(max_value.to_string()))
    
    # More complex ternary expressions
    var status = "positive" if number > 0 else ("zero" if number == 0 else "negative")
    println("Number status: ".append(status))
    
    # While Loop
    println("=== While Loop ===")
    
    var counter: i32 = 1
    println("Counting from 1 to 5:")
    
    while counter <= 5:
        println("Count: ".append(counter.to_string()))
        counter += 1
    
    println("Counter after loop: ".append(counter.to_string()))
    
    # Break Statement
    println("=== Break Statement ===")
    
    var search_target: i32 = 7
    var search_counter: i32 = 1
    var found: bool = false
    
    println("Searching for ".append(search_target.to_string()).append(":"))
    
    while search_counter <= 10:
        println("Checking: ".append(search_counter.to_string()))
        
        if search_counter == search_target:
            println("Found target!")
            found = true
            break  # Exit the loop immediately
        
        search_counter += 1
    
    if not found:
        println("Target not found")
    
    # Continue Statement
    println("=== Continue Statement ===")
    
    println("Printing only odd numbers from 1 to 10:")
    
    var j: i32 = 0
    while j < 10:
        j += 1
        
        if (j rem 2) == 0:
            continue  # Skip the rest of the iteration for even numbers
        
        println("Odd number: ".append(j.to_string()))